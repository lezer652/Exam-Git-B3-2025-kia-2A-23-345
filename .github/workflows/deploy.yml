name: Deploy to Production

on: 
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Afficher Début du déploiement
        run: echo "Début du déploiement"

      - name: Récupérer et afficher le secret DEPLOY_TOKEN
        run: echo "DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}"

      - name: Afficher Déploiement terminé
        run: echo "Déploiement terminé par Alain-Geremy Thieba Nambogona-Ngbanga"
